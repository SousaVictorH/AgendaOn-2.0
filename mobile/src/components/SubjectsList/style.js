import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    list: {
        marginTop: 32,
    },
    container: {
        padding: 24,
        margin: 4,
        flexGrow: 1,
        borderRadius: 8,
        backgroundColor: "#FFF",
        marginBottom: 16,
        display: "flex",
        flexBasis: 0,
        maxHeight: 220,
    },
    Propriety: {
        fontSize: 14,
        color: "#41414d",
        fontWeight: "bold",
    },
    Value: {
        marginTop: 8,
        fontSize: 15,
        marginBottom: 24,
        color: "#737380",
    },
});

export default styles;
