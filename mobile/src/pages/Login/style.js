import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    background:{
      flex: 1,
      alignItems: "center",
      backgroundColor: '#000',
    },
    header: {
      flex: 1,
      alignItems: 'center',
      width: '100%',
      maxHeight: 50,
      marginTop: 115,
    },
    headerText: {
      color: 'blue',
      fontSize: 32,
    },
});

export default styles;
