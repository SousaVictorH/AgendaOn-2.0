import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    background:{
      flex: 1,
      alignItems: "center",
      backgroundColor: '#000',
      width: '100%',
    },
    header: {
      marginTop: 32,
    },
    headerText: {
      color: '#fff',
      fontSize: 32,
    },
});

export default styles;
